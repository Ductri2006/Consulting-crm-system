import { formatDate, formatDateTime, formatFileSize } from '../../i18n/format'
import { fallbackEnumLabel } from '../../i18n/statusLabels'

export const formatPortalLabel = fallbackEnumLabel

export const formatPortalDateTime = formatDateTime

export const formatPortalDate = formatDate

export const formatPortalFileSize = formatFileSize
